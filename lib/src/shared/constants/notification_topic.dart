/// proper topics
/// [
///   gender,
///   all
///   dptCode,
///   faculty,
///   enrollment year
/// ]
///

// ? this is redunancy
class NotificationTopic {
  static const String all = 'all';
  static String get(String studentGender) => studentGender;
}
